query distribution($id: String!, $blockHash: Bytes!){
    distribution(id: $id, block: { hash: $blockHash }){
        id
        distributor{
            id
        }
        timestamp
        distributionNumber
        merkleRoot
        metadataURI
    }
}
