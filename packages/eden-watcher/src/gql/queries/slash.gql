query slash($id: String!, $block: Block_height){
    slash(id: $id, block: $block){
        id
        timestamp
        account{
            id
            totalClaimed
            totalSlashed
        }
        slashed
    }
}
